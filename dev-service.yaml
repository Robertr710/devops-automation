apiVersion: v1
kind: Service
metadata:
  name: dev-app-service
spec:
  selector:
    app: dev-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
